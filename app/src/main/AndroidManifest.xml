<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aeropaymerchantsdk">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ValidatePinActivity"></activity>
        <activity android:name=".activity.ConfirmPinActivity" />
        <activity android:name=".activity.SetPinLogin" />
        <activity android:name=".activity.HomeActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".activity.FastLoginActivity" />
        <activity
            android:name=".activity.SettingsScreenActivity"
            android:configChanges="orientation|screenSize|keyboardHidden" />
        <activity android:name=".activity.NavigationMenuActivity" />
        <activity
            android:name=".activity.BaseActivity"
            android:configChanges="orientation|screenSize|keyboardHidden" />
        <activity android:name=".activity.SignInCredentialActivity" />
        <activity android:name=".activity.SignInScreenActivity" />
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>